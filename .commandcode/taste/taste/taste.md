# Taste
- Communicates in Chinese (Simplified) when giving task instructions; expects responses in the same language. Confidence: 0.7
- During refactoring, wants changes scoped narrowly to the requested class/component (e.g., "不需要处理这个类以外的错误") — unrelated compile errors from other incomplete parts of the refactor should be treated as out of scope, not fixed or chased down. Confidence: 0.9
- Looks to sibling/similar projects (e.g., the Summoner mod) as the authoritative pattern reference when implementing comparable features (e.g., item registration) and expects the code to follow that established pattern ("参考同类项目Summoner的注册模式"); will actively point out the reference project's location when the assistant cannot find it — including the in-house Lyra framework's own source repo (D:/IDEA/lyra) when the assistant is about to decompile its jar instead of reading the real source. Confidence: 0.9
- Prefers using the shared Lyra library's helper builders (e.g., LyraItemRegisterBuilder) to register items and to drive generation of the related language-file/tooltip entries, instead of hand-written registration code plus separate manual lang-provider code. Confidence: 0.7
- Within Lyra's registration builder, prefers its dedicated typed methods over composing raw lang keys by hand — e.g., use `itemLanguageTooltip(index, en, zh)` for tooltip lines so the dependency library auto-generates the `item.<modid>.<path>.tooltip.<index>` key, rather than writing the key string manually. Confidence: 0.8
- When a dependency library natively auto-handles a runtime feature (e.g., Lyra's TooltipHandler automatically appends every `item.<mod>.<path>.tooltip.<N>` key, numbered contiguously from 1, to an item's tooltip), prefers consolidating all per-item text into that library mechanism — including merging previously shift-gated `attribute.N` skill text into the continuous `.tooltip.N` sequence — instead of keeping the consuming mod's parallel hand-rolled display logic that would duplicate output. Confidence: 0.6
- When organizing large registrations of hierarchical items, wants the register file itself laid out in ordered, named comment-banner segments mirroring the mod's item hierarchy (each force-level's sub-stones grouped as one segment, all force-level stones as one segment, and the Terra Soul as the final soul segment) rather than a flat listing. Confidence: 0.6
- Before undertaking large-scale migrations/refactors with high rework cost, prefers the assistant to surface architectural forks and ask clarifying questions with concrete options (e.g., how to handle missing types, key-naming conflicts, where segmentation should live) rather than guessing. Confidence: 0.8
- For large content migrations (e.g., moving hundreds of lang entries from a provider into a registration builder), insists the moved content be verbatim — preserve existing keys and exact en/zh text, do not fill in gaps, rename keys, or "improve" text while relocating. Confidence: 0.8
- When a requested rename would cascade across asset files (textures), recipes, and hardcoded code references (e.g., renaming every registration id to match English names with `_enchantment`/`_power`/`_soul` suffixes), prefers to keep internal registration identifiers stable and limit the change to the user-visible display-name/translation layer instead ("不改注册名，只改翻译名称"). Confidence: 0.7
- Wants English display names to be systematically consistent with the code's class/registration-name root plus a type suffix (BlazeSoul → "Blaze Enchantment", DeathPower → "Death Power", TerraSoul → "Terra Soul") rather than lore-based Terraria names ("Solar Enchantment", "Power of Death"); treats the Chinese display layer as an independent one that stays untouched when only English names are realigned. Confidence: 0.6
- Organizes creative-mode tabs via the sibling project's tag-driven Lyra Section mechanism (a `Section` record with order/banner/AnimBanner/tag, registered through `CreativeTabDispatcher.register(tab, Section...)`) instead of manually populating `displayItems`; each section collects its items purely from an item tag, so every item that must stay visible in the tab (including non-accessory materials/blocks) gets a section tag and non-article items get their own "basic" section (order 0). Confidence: 0.8
- Items that equip through Curios need the Curios slot item tag (`.itemTag(curios:curio)`) added in the Lyra builder chain on top of implementing the curio interface, because Curios slot fitting is tag-driven; follows the sibling project's accessory builder ordering — `.itemTag(section)` placed near the language lines and `.itemTag(Curio)` just before `.build()`. Confidence: 0.7
- For soul/accessory item tooltips, wants shift-gated display: with Shift NOT held show only a hint line ("按住 [Shift] 可查看概要", reusing the existing `key.shift.tooltip.1/2` keys with a gray literal "Shift" between them); full details only appear while Shift is held. Confidence: 0.8
- Shift-detail layout for hierarchical soul items: the first line enumerates ALL direct child soul items by their translated names in a "获得xx魔石、xx魔石……的效果" style (direct children from `getSoulItemList()`, not flattened descendants), then a blank separator line, then the item's own skill/stat lines; the own lines must follow the Lyra tooltip format — contiguous `item.<ns>.<path>.tooltip.N` keys read from 1 until the sequence breaks, rendered DARK_GRAY. Confidence: 0.7
- Implements tooltip display by putting the logic in the item class itself as a `getTooltip(TooltipFlag)` method on SoulItem and routing Lyra's tooltip through it via a client Mixin that intercepts the Lyra `TooltipHandler` output for these items — rather than a NeoForge `ItemTooltipEvent` listener or logic scattered in a client event class; display lives in the item, per-line text data stays in Lyra-generated `.tooltip.N` lang keys. Confidence: 0.7
- Treats the per-item language/tooltip text recorded in the register file (e.g., FargoSoulItemRegister) as the authoritative behavior specification for that item — "根据FargoSoulItemRegister中的语言文本完成代码" means implementing exactly what the (zh) tooltip lines describe (values, thresholds, cooldowns, passives), even when the old pre-refactor implementation of the same item did something different; outdated code is not a reference, the lang text is. Confidence: 0.7
- Explicitly sanctions consulting the mod's own pre-refactor git-history implementation as a direct mechanism/value reference for specific items whose new tooltip still matches the old behavior ("IronSoul和铅中毒的旧代码可以直接参考") — old code is rejected as a reference only when it diverges from the current lang-text spec, not categorically. Re-requests for a specific old-version mechanic (e.g., "铅中毒添加旧版的传染效果") arrive with no spec detail and expect the assistant to fetch that legacy logic itself and port it. Confidence: 0.7
- When completing a class in the post-refactor architecture, implement it by mirroring the already-migrated sibling classes (the sub-soul pattern: inner `SoulInfo` state class registered in `SoulInfoRegister`, tick-driven accumulation with `SoulItemData.isEquipped` removal, NBT serialize/deserialize, ability hooks as `SoulItem` overrides such as attack/hurt/curioTick) rather than the pre-refactor architecture's leftover patterns. Confidence: 0.7
- Names the per-soul inner state classes uniformly `Info` with no soul-name prefix (e.g., `BlazeSoul.Info`, `CosmicPower.Info`, never `BlazeSoulInfo`/`CosmicPowerInfo`), including the generic references in `SoulInfoRegister`; enforces this across the whole soul family when touching it. Confidence: 0.9
- Treats the inner `Info` (SoulInfo) classes as pure data stores: their `tick` should only check removal conditions (e.g., item no longer equipped → `setRemove(true)`) to prevent memory leaks, and must not carry gameplay/business logic ("info的功能主要是辅助数据存储，tick检查条件remove防止内存泄漏，不应该放置过多逻辑"). Confidence: 0.9
- Draws a semantic distinction for the removal condition in `Info.tick`: cooldown-type Infos (e.g., StardustSoul/VortexSoul — a skill-triggered cooldown timer) must NOT be removed on unequip; their tick is only the natural countdown (`--cooldown <= 0 → setRemove(true)`), so the cooldown persists on the entity through unequip/equip and cannot be refreshed by unequipping and re-equipping ("冷却类使用不应该在卸下后移除"). Only persistent/resource-type Infos (energy states like CosmicPower/BlazeSoul/NebulaSoul) bind removal to `SoulItemData.isEquipped`. Confidence: 0.8
- Wants gameplay/business logic relocated from the Info classes into the owning `SoulItem`'s overrides — per-tick behavior (energy accumulation/drain, cooldown countdown, healing) lives in `SoulItem.tick(LivingEntity)`/`attack`/`hurt`, not in `Info.tick`; a structural refactor must preserve the original behavior exactly (only move code, not change values/mechanics). Confidence: 0.7
- Damage-modifier semantics (current final state, supersedes the two earlier conventions): the user explicitly rejected distinguishing "original vs latest" damage bases as too troublesome ("区分原始和最新还是太麻烦了"). `SoulItem.attack/hurt` hooks now carry only the running chained value (`damage`) — the `originalDamage` parameter was removed from the base class and all cosmic-power overrides. Every percentage effect is multiplicative on the current value and worded accordingly: boosts are "提升至 X%" (`damage *= X/100`, e.g. Stardust frozen "造成的伤害提升至200%" = `damage *= 2`; CosmicPower "最高提升至180%" = `damage *= 1 + 0.8*ratio`); damage reduction stays "减少 X%" (`damage *= 1 - X%`); only fixed-value additions (flat amounts like Nebula's "附加你4%最大生命值") remain additive because they are not percentage boosts. Tooltip wording is updated in lockstep with code semantics. Confidence: 0.9
- The damage-base convention flip-flopped across sessions (original-based additive → latest-value "提升至 X%" → original-based → back to latest-value "提升至 X%"); treat the latest-value-multiplicative system as settled and do not re-introduce an `originalDamage` parameter or an original-vs-latest distinction without an explicit new instruction. Confidence: 0.8
- Prefers simple, uniform mechanic designs over precise-but-complex ones: chose one uniform multiplicative base over a semantically exact dual-base system because maintaining two semantics was too troublesome. Favor the least-complex consistent rule when proposing damage-modifier/mechanic designs. Confidence: 0.7
- Implements special effect entities (non-rendered, summoned-by-item effects like the Vortex) as Lyra "virtual entities" (the project's self-developed attachment-entity system, no renderer needed) rather than vanilla/NeoForge rendered entities; points to an existing in-project sibling class (Meteor) and the Summoner sibling project as the authoritative reference for how such entities are structured. Confidence: 0.8
- Treats entity-type registration and render registration as a required binding pair for every virtual entity class in common/entity (SummonerAttachmentEntityRegister + SummonerAttachmentEntityRenderRegister); pure logic/auxiliary entities (e.g., Sprint, used only for sprint-assist collision detection) are explicitly bound with a null renderer ("不进行渲染") rather than being skipped in the render register or given a visual renderer. Confidence: 0.7
- Wants particle effects driven through the Lyra particle API — `ParticleHelper` controlling `GenericParticleBuilder` — rather than vanilla particle spawning. Confidence: 0.6
- For projectile summons (e.g., SnowBall), wants a small particle burst at BOTH the launch/spawn point and on impact ("产生SnowBall和命中时，爆出一点粒子"), and points to the mod's old git-history implementation as the authoritative reference for how that particle burst should look/fire (same particle type, `ITEM_SNOWBALL`), porting that old behavior into the new `ParticleHelper` (`.type().pos().count().speed().spread().offset().emit()`). Confidence: 0.5
- Data-ownership rule for the SoulInfo/attachment sync layer, stated as a two-sided split: pure-client data is client-local only and must survive server sync/deserialization (server data must never overwrite or clear it) and must be excluded from server-side NBT saves and sync packets; ALL non-pure-client data, by contrast, is fully cleared and rebuilt from the server snapshot so server-removed entries cannot linger on the client — the client's resulting state is "client-owned data preserved + server snapshot replaces everything else" ("纯客户端数据由客户端自己控制，其他数据完全清理后由服务端数据覆写"). Confidence: 0.8
- The ownership marker is a type-level property, not per-instance state: explicitly directed to move the pure-client flag off the instance method (`SoulInfo.isClientSide()` deleted) onto the registered type itself — a `clientSide` boolean component of the `SoulInfoType` record set at registration (Sprint/Fly registered as `clientSide=true`). Tick dispatch and (de)serialization/sync filtering all read the flag from the entry's type key, since data ownership belongs to the data kind, not to individual instances. Confidence: 0.9
- The server-side tick handler that mutates the SoulInfoData attachment is the point where removals must be pushed: after the `removeIf` sweep of expired entries (each entry `tick()`ed, removed when `isRemove()`, with `onRemove()` called before removal), the handler ends with `living.syncData(SOUL_INFO_DATA)` — explicit sync at the mutation site rather than relying on login/initial attachment sync, since mid-session server removals otherwise never reach the client and linger there. Confidence: 0.8
- The tick handler must run on BOTH logical sides, each side ticking only the data kind it owns — server ticks only `clientSide=false` entries, client ticks only `clientSide=true` entries (`soulInfo.getType().clientSide() != clientSide → skip`), with `syncData` fired only from the server side; the two sides manage disjoint data and never tick each other's types. Confidence: 0.9
- Communicates the expected implementation by pasting a concrete, runnable code snippet as the whole message (no prose); the pasted code is the spec to adopt and build on as-is (here: `EntityTickEvent.Post` guarded server-side via `instanceof LivingEntity` + `!level().isClientSide()`, `removeIf` over the attachment's `data().values()`) rather than something to redesign or replace. Confidence: 0.6
- For a deserialize path shared between client sync and server NBT load, the "clear non-client-owned, keep client-owned, rebuild from incoming data" rule is safe on both sides precisely because the server never holds client-only types in the first place — filtering by the type flag in `deserializeNBT` yields identical, correct semantics when reading either a sync packet (client) or a save file (server). Confidence: 0.6
- The behavior-preservation rule applies generally to cleanup rewrites of leftover old-architecture code inside new-architecture classes, not just to Info/game-logic relocation: rewriting SoulRarity's tick-driven gradient coloring for special rarities (-1 Expert / -2 Master) must keep the observable final effect identical ("在不改变最终效果的情况下进行重写"), while the user explicitly invites finding a more concise formulation in the process ("看看有没有更简洁的写法"). Confidence: 0.7
- For derived per-tick values that vary by a category key (e.g., rarity gradient colors for -1/-2), prefers the computed results to be stored in a map keyed by that category ("改为写入map") rather than held per-instance or recomputed through imperative branches in tick. Confidence: 0.5
- Keeps display-only animation strictly separate from persisted data-component state: purely visual dynamic effects (e.g., animated gradient colors for sentinel rarity values -1/-2) must never mutate the stored component — the record's canonical accessor (`color()`) continues to return the true persisted value (the Codec/StreamCodec serialize through it, so overriding it to return an animated color would corrupt saves/sync), while display reads go through a separate getter (`getColor()`) that resolves the client-side cached display color; the client-only tick updates only that cache, never the component ("我改为了getColor防止组件真实数据改变，此颜色只作为展示效果，不改变真实数值"). Confidence: 0.9
- Naming style for static constants: prefers PascalCase (like `Purple`, `Expert`, `Master`) over ALL_CAPS / UPPER_SNAKE_CASE (e.g., GRAY, CODEC, STREAM_CODEC) — explicitly instructed to rename SoulRarity's constants to "像Purple这样的格式，不要全大写", including naming bare sentinel literals (-1/-2) as named constants in the same style. Confidence: 0.7
- Tooltip lang-key index sign convention in this mod's soul items: positive indices (1, 2, 3…) are the Shift-held mechanic/description lines (the `getTooltip` reads them ascending from 1 when Shift is down), while negative indices (-1, -2, -3…) are the easter-egg/atmosphere quote lines shown when Shift is NOT held, read descending from -1 (`for (int i = -1; I18n.exists(key + i); i--)`). When the user says "逻辑端已经完成" and pastes that getTooltip, the pasted logic is the authoritative spec and register/lang data must be renumbered to match it — easter-egg lines go to -1, -2, … (multi-line eggs keep their reading order as -1 then -2, matching the descending loop), never left on positive indices. Confidence: 0.7
- For per-player damage-source identity of a soul item, wants the UUID to be a deterministic derivation from BOTH the soul item and the player rather than a bare player UUID ("以SoulItem和玩家uuid构建一个稳定的uuid") — same player + same soul item must always yield the same stable UUID (unchanged across reconnects/re-entry), while different items or different players yield distinct values so each soul's damage can be tracked independently (e.g., per-UUID i-frames). Confidence: 0.6
- For effect-entity visuals when the mod lacks a dedicated texture/model asset, prefers to skip sprite rendering entirely ("暂时不渲染贴图") and convey the effect with light/particles only — e.g., the shadow orb renders through a light-only renderer (`simpleRenderer(null, 0, 12)` → only `DynamicLightDispatcher.addLightSources(pos, 12)`) rather than reusing an unrelated existing texture (like the Meteor glow sprite tinted purple) as a placeholder; dedicated visual assets can be added later. Confidence: 0.5
- Code style: avoid extracting single-use constants for gameplay values/durations when not necessary — inline literals directly ("非必要不写常量（如持续时间、数值等直接内联字面量）"), instead of hoisting e.g. `PREEMPTIVE_DURATION = 20` / `ARMOR_BREAK_DURATION = 200` style named constants for one-off usages. Confidence: 0.7
- Comments should stay minimal: no long javadoc blocks on classes/methods ("类/方法上的大段 javadoc 不需要"); at most keep a single short explanatory comment line before registration entries ("注册项前可保留一行简短说明"), e.g., a one-line zh note describing the effect above its effect/item registration. Confidence: 0.8
- When the user personally rewrites an implementation and tells the assistant to learn their style ("我修改了代码，你需要学习我的写作风格"), the hand-edited code becomes the authoritative spec: do not revert or "fix" the mechanics back to the previous implementation; instead confirm any ambiguous semantics (e.g., changed damage multiplier, moved trigger point) via targeted questions and align stale comments, register values, and tooltip text to the user's version. Confidence: 0.7
- Feature ownership across soul items: when a soul's effect only conditions on a status that a different soul applies (e.g., DarkArtistSoul benefits against shadow-flame targets while applying shadow flame is GloomySoul's role), implement the soul as purely conditional — do not make it self-apply the prerequisite effect just to be self-sufficient, even if that leaves the condition dormant until the supplying soul is finished. Confidence: 0.6
- Code style: generally avoids early `return` exits in favor of nested `if` structures ("一般不使用提前 return 提前退出，而使用 if 嵌套结构") — e.g., instead of a top-of-method `if (!condition) return;`, wrap the logic in `if (condition) { ... }`. Confidence: 0.8
- Implements continuous damage-over-time / health-drain status effects via the lyra HealthRegen attribute with a negative curve modifier (`amplifier -> -(amplifier+1)*系数`) rather than manually calling `hurt` in `applyEffectTick` gated by `shouldApplyEffectTickThisTick`. Confidence: 0.8
- For latency-sensitive ability state that must apply on the next action without waiting for a packet round-trip (e.g., "press penetrate key → the next sprint becomes phase-through"), has BOTH the client and the server hook of that action write the same self-decrementing marker — the client writes first ("client 提前添加，防止同步不及时") so the effect is locally active before the server's copy exists, and the server writes when the packet arrives. Confidence: 0.7
- For such dual-side ability states, prefers ONE server-authoritative (`clientSide=false`) SoulInfo that syncs to the client over maintaining two parallel client/server info types: the client may locally write pending/timer fields in advance (in `sprintClient`) and the server's later `sprintServer` write + syncData overwrite reconciles them; the small race window this implies is accepted by design. Confidence: 0.6
- Implements block-phase / pass-through abilities as empty collision boxes: inject into `BlockBehaviour.BlockStateBase#getCollisionShape` via a matched pair of mixins — `ClientBlockStateBaseMixin` (matches LocalPlayer) and `ServerBlockStateBaseMixin` (matches ServerPlayer) — and return `Shapes.empty()` when the player's active-state info (queried via SoulInfoData) says the effect is on, so both logical sides agree on wall-ignoring. Confidence: 0.7
- For revive-on-death mechanics hooked into a cancellable death event, first verifies the death was NOT already canceled by another effect (`canceled == false`) before consuming the revival resource and restoring health; only after that check passes does the hook itself cancel the death (return false) — don't burn the revive on an already-prevented death. Confidence: 0.6
- Issues extremely terse, single-phrase rework directives for a soul item (e.g., "NinjaSoul重做。") with no spec details whatsoever; expects the assistant to autonomously derive the complete rework target from authoritative in-repo sources — the item's (zh) tooltip/lang text in the register file, which can describe an entirely different mechanic than the current code — and then implement, register the state type, and compile-verify end-to-end without asking clarifying questions for such a scoped item rework. Confidence: 0.5
- When an entity mirrors one that already has a dedicated renderer in the sibling project (LightningOrb ↔ Summoner's BlitzBallRenderer), that renderer must be reproduced rather than bound with a null/placeholder renderer: pure-code visuals via Lyra helpers (SphereRendererHelper glowing sphere + LightningRendererHelper arcs to the entity's idList targets + DynamicLightDispatcher), no texture/model assets required, registered by constructor reference (LightningOrbRenderer::new); the null-renderer convention applies only to entities with no visual counterpart to copy. Confidence: 0.6
- Launches player projectile attacks from the caster's eye position (`getEyePosition()`) with direction computed as target bounding-box center minus eye, normalized — not spawned at/above the target with zero velocity; multi-projectile volleys keep the shared eye origin and scatter each shot's aim point slightly (±0.5). Confidence: 0.7
- For generic cross-cutting rule effects that key off a status on the entity regardless of which item applied it, prefers standalone event handlers in the central Event class over logic embedded in soul-item hooks or effect classes: e.g., heal-reduction statuses (Bleeding −50%, Hemorrhage −70% via `LivingHealEvent.setAmount`) and Oiled-status fire damage boost (×3 via `LivingIncomingDamageEvent` + `IS_FIRE` tag) are each their own `@SubscribeEvent` agnostic of the applying soul item ("采用独立事件监听实现效果"). Confidence: 0.6
- Helper-minion summons that orbit the player (e.g., magic shadow orbs orbiting the wearer like Summoner's EtherealStellarCore) are added to Lyra's `AttachmentEntityData.Type.ExtraMinion` with `slotCost 0` so they do NOT consume the player's regular minion cap; the owning soul item replenishes one per tick until the target count is reached ("不足3个时添加1个"), and each minion self-checks in its own tick that the granting soul is still equipped (`SoulItemData.isEquipped`) and calls `setRemove()` when it is not. Confidence: 0.5
- Lyra's minion-add is DEFERRED (pendingAdd 延迟队列): the group size does not update within the same tick, so refilling summoned minions (crystals, blades, etc., on any AttachmentEntityData type) must add at most one per tick via `if (size < N) { add one }` — never a `while` loop, which reads a stale size forever and spins ("Lyra的添加召唤物是延迟模式，不能使用while，应该使用逐tick检查补齐"). Confidence: 0.8
- For virtual-entity projectiles that have no model/texture assets, visual style is chosen per projectile and built from Lyra render helpers: a custom independent renderer draws a "droplet" red trail as shrinking spheres along the entity's history nodes when a trail is wanted (tracking blood orb); a plain tiny sphere via `SphereRendererHelper` when explicitly no trail (damage blood drop); a simple renderer referencing a vanilla sprite path (e.g. `minecraft:textures/item/snowball.png`) as a camera-facing billboard when a vanilla item sprite fits ("雪球使用简单渲染器…告示牌渲染"); and a null renderer only as an explicit placeholder ("星星暂时使用简单渲染器null占位"). Confidence: 0.5
- The user actively edits the same files in their IDE while the assistant is working — files repeatedly change between assistant operations ("文件在我读取后被外部修改了" / "外部又改了…"); treat earlier reads as stale and re-read the current file content immediately before editing or overwriting it, rather than editing from memory of a prior version. Confidence: 0.5
- For bulk implementation of a large set of similar items whose spec descriptions were just rewritten by the user ("我已经全部完成描述重构"), wants a simple-first execution order: implement all simple, fully-specified items first in batches without per-item clarification, and defer the complex/unclear ones to be refined through individual follow-up questions afterwards ("先完成简单的，复杂或者描述不清晰的等做完简单的，进行每一个单独追问细节进行完善") — don't ask about everything up front, and don't guess on ambiguous spec items mid-batch. The user actively drives this tiered workflow batch by batch (approving the simple batch, then tersely directing "推进下一批中等魔石" / "继续下一批候选" to continue into the next difficulty tier rather than pausing for questions). Confidence: 0.9
- Within an approved medium-difficulty batch, prunes items that depend on an unbuilt shared mechanism (fly hooks, custom entity projectiles, special effects, block events) out of the current batch and defers them to a later dedicated pass alongside that mechanism, so the batch stays limited to items realizable with existing hooks/effects/attributes only (e.g., the medium batch was scoped to Mithril/Adamantite/RedRiding/Turtle, deferring Beetle/Bee to the fly pass and Teke/Orichalcum/Pumpkin to effect/entity passes). Confidence: 0.6
- When a mechanism is under-specified in the user's tooltip/spec, first traces how that mechanism is consumed by the codebase (e.g., grep `getMaxFlyTime`/`fly(` call sites to see how a fly system is fed before implementing fly-granting items) rather than guessing the integration point. Confidence: 0.5
- Passive attribute stat bonuses (movement speed, attack speed, armor, summon damage/armor penetration, etc.) must be implemented by overriding `SoulItem.getAttributeModifiers(Player)` to return the `Multimap<Holder<Attribute>, AttributeModifier>` — `SoulItemData` re-applies it every tick under a `_volatile`-suffixed modifier id, so dynamic values work — and NOT via `tick` + `AttributeUtils.condition(...)`; explicitly corrected the tick+AttributeUtils approach used for such stat passives ("被动属性加成…应覆写 SoulItem.getAttributeModifiers(Player)…不要用 tick + AttributeUtils"). Confidence: 0.9
- Callers of `SoulTargetCache.getEntitiesInRadius` should NOT pass `SoulTargetCache.isTarget` as the entity filter — the cached list is already pre-filtered to valid targets at cache-tick time; pass `null` as the filter, and only supply a custom filter when an extra condition applies (e.g., `other != target`). Confidence: 0.8
- When several percentage-boost effects are simultaneously active across a hierarchy (a parent soul implicitly includes its child souls), prefers plain multiplicative stacking of EVERY contributor ("全部累乘") over taking the largest bonus — selected full multiplication even when a max-cap option was offered as recommended, accepting that totals escalate with inclusion. Confidence: 0.6
- For a shared per-tick capability budget (e.g., flight), the user's explicit design is: the capability-granting hook (`canFly`) supplies the base budget (100 ticks when flight is granted), and each soul's `getMaxFlyTime` contributes a chained multiplicative bonus chain applied against that base (mirroring the sprint aggregation pattern); the budget is active only when at least one equipped soul actually grants the capability — not per-item flat additive seconds, not an always-on budget, and the base is 100 ticks (explicitly corrected from an assumed 600). Confidence: 0.8
- For status effects that exist purely as markers whose real behavior is evaluated elsewhere (e.g., Midas marker checked on kill for gold drop; Ambrosia marker checked via `hasEffect` by the owning soul's hooks/attribute modifiers), registers them as empty marker MobEffects (no behavior/tick logic in the effect class) instead of behavior-bearing effects. Confidence: 0.5
- When multiple large deferred features remain and the assistant asks which to do first (e.g., "先水晶或先剑？"), the user prefers they all be implemented together in one combined pass ("一起做NaturePower和SpiritPower") rather than picking a sequencing order and checkpointing — the same keep-moving-without-pausing-for-sequencing-questions pattern as the tiered batch workflow. Confidence: 0.4
gn/semantic forks ("继续推进，遇到无法确定的问题再询问"). Observed pattern: numeric gaps (explosion damage/radius, heal amounts, check intervals, durations) are defaulted without asking, while questions are reserved for choices like new entity vs reuse of an existing one, ambiguous spec wording (e.g., what "溢出" means), and per-unit extension rates. Confidence: 0.7
- For asset transformations that are slated to be repeated across many files (e.g., downscaling a whole texture set 48×48→24×24), the user wants a feasibility test on ONE representative sample first, with an explicit user go-ahead before any batch processing ("你先测试，如果可行我会让你压缩其他贴图") — never mass-process other files until the approach is proven on the sample and confirmed. Confidence: 0.8
- Quality bar for image downscaling is true losslessness ("压缩应该可以无损"): the user expects the assistant to actually verify with pixel-level analysis — 2×2 block uniformity, unique palette count, and a downsample→upsample roundtrip restore percentage — not assume losslessness, and to report honestly with decision options when a lossless shrink is impossible (here: 39% non-uniform 2×2 blocks from single-pixel-width antialiased lines → roundtrip restore only 82.2%) rather than silently producing a lossy result. Confidence: 0.6
- For beam-attack minion entities that must damage along a line between themselves and a locked target (chlorophyte crystals), prefers NOT spawning a separate custom laser projectile entity ("不召唤新自定义激光射弹"): damage comes from the entity's own dynamic collision box — a long hitbox extending toward the target (Terraprism/CustomLaser-style), while the laser is only rendered between the entity and the target at a fixed radius. Confidence: 0.6
- When porting an entity's visuals from the sibling Summoner project, keep the renderer minimal and faithful to the base model: render only the body model itself — orbiting bodies keep their own rotation animation instead of the path node's pose ("渲染模型本体保持旋转，不使用真实路径节点的姿态进行渲染"), and Terraprism-based summons render with just the base model, no dynamic color/tint ("渲染器不需要加动态颜色，只渲染泰拉棱镜本体模型"). Confidence: 0.6
- Sanctions copying model/texture assets from the sibling Summoner project into this mod's resource tree when a feature must use that model (e.g., terraprism/chlorophyte_crystal json + png), rewriting only the namespace (`summoner:` → `fargo_soul:`) in the copied json rather than hand-recreating the model file. Confidence: 0.5
- Per-second health recovery (每秒生命恢复) is a Lyra `HealthRegen` attribute concern, not a manual-heal concern ("每秒生命恢复是一种Lyra的一种属性，而不是手动恢复"): conditional/timed regen passives (Ambrosia +5/s, mounted +2/s, near-flower +2/s) are implemented by putting `HealthRegen` ADD_VALUE modifiers in `SoulItem.getAttributeModifiers` (recomputed every tick against its condition) and removing the manual `heal()` calls from `tick`. Confidence: 0.9
- Damage-reflection abilities must be protected against infinite recursion (two reflectors bouncing damage back and forth) with a built-in ~0.1s (2 tick) cooldown on the reflect action ("反弹需要做防递归处理，内置0.1秒冷却时间") — track a `lastReflect` tick in the soul's per-player Info and only reflect when `tickCount - lastReflect >= 2`, instead of reflecting unconditionally on every incoming hit. Confidence: 0.8
se, modifiers)` ("healAmount方法改为通过List<ValueModifier>模式"). Souls that only consume the raw amount (e.g., Palladium's energy accumulation on heal) keep using the `amount` parameter and add no modifier. Confidence: 0.8
- Lyra attachment-renderer pipeline semantics ("render是已经经过姿态变换后的方法，应该使用modelModify，参阅AbstractAttachmentEntityRenderer"): `render()` runs after the node's pose transformation has been applied to the stack, so any world-coordinate geometry — e.g., a two-point laser drawn via `LightningRendererHelper` from the entity's actual position (`visualNode.pos()`) to the target's bounding-box center — must be drawn in `modelModify` (the pose-construction stage), where the stack still carries only camera translation and world coordinates convert correctly. The oriented model is then drawn in the same phase with a manually composed matrix (yaw/pitch from the visual node for aiming + roll self-spin over tick time, then scale/center translate), and `render()` is left empty rather than carrying post-transform drawing. Confidence: 0.8
- For summoned combat helpers that must auto-acquire enemies, subclass the Lyra `Minion` base class and override its targeting hooks (`getSearchDistance()`, `searchTarget()` — random pick from `SoulTargetCache.getEntitiesInRadius`, `isTarget()` via `SoulTargetCache.isTarget`) so Minion's own tick maintains the target automatically ("继承Minion以获得自动索敌"), rather than hand-rolling target fields/refresh inside the entity's tick. Confidence: 0.7
- When multiple accessories summon the SAME entity class for independent roles (GreenSoul's single head crystal vs NaturePower's five orbiting crystals), instances carry a `kind` discriminator and every group-sensitive operation filters by kind — orbit phase (`kindOrder/kindTotal/kindList`) and each spawner's refill count (Nature counts only NATURE orbs, Green only GREEN), never all instances of the type — so the groups' counts cannot interfere ("叶绿魔石和自然之力的水晶数量不能互相影响"); unequip self-removal likewise keys on kind and checks each group's own accessory. Confidence: 0.7
- When a summoned entity deals damage under its own entity UUID (not the player's), it must also write the owner player into the target's hit history via `InvincibleData.get(target).recordHit(owner.getUUID(), 100)` before applying the attack, so the player stays the recorded aggressor for target determination ("造成伤害后…用InvincibleData的recordHit写入玩家的历史uuid保持目标判定"). Confidence: 0.7
- For orbiting/attached minions whose visual position must not lag behind the server node, override `getRenderNode(partialTick)` to provide a render-fitted node: recompute the orbit position from `partialTick`-interpolated owner motion (`xo→x`, `yo→y`, `zo→z`) plus per-kind orbit phase, and set yaw/pitch to aim at the current target from that interpolated position ("覆写getRenderNode，以实现渲染拟合"); the client then renders smoothly without per-tick server node updates. Confidence: 0.6
- Strict separation between logical aim and visual pose for minion models: yaw/pitch aimed at the ATTACK TARGET (used only for logic/collision: beam hitbox, laser endpoints) must NOT be baked into the rendered model's rotation ("实际逻辑指向，不能体现在渲染上") — the model never visually points at the enemy. This does NOT mean the orbiting model is an unoriented pure self-spin: when the entity circles the player via the orbit-node provider, it must ALWAYS face the player/owner while rotating ("getOrbitNode周身旋转时需要始终面向玩家") — the node pose is not fixed (0,0,0); its facing is recomputed from the orbit geometry so the model continuously turns toward the player's center as it orbits, distinct from combat-target aiming (which stays logical-only). Confidence: 0.7
- When a `Minion`-derived entity is stored in an `ExtraMinion` attachment group rather than `Type.Minion`, override the base class's four order/size accessors so Lyra's cache machinery works for the actual group ("重写父类的方法，以获得缓存支持"): `getOrderCache()`/`getSameSizeCache()` return the synced cache fields (`order`/`sameSize`) that Lyra's deferred slot update fills via setOrder/setSameSize, and `getOrder()`/`getSameSize()` dynamically query the real storage group — `AttachmentEntityData.Type.ExtraMinion` + `getType()` (`indexOf(this)` / `size()`) — since the Minion base's `Type.Minion` query yields −1/0 for ExtraMinion-stored minions. Confidence: 0.7
- For beam/laser visuals from a summoned minion toward its locked target, explicitly prefers Lyra's `LaserRendererHelper` over `LightningRendererHelper` ("使用LaserRendererHelper而不是LightningRendererHelper"). LaserRendererHelper is length-based (renders along its own local -Z axis via a builder chain `.length().radius().layers().segments().color().alpha().innerRatio().render()`; it has no two-point world-coordinate API like Lightning does), so its direction must come from the pose stack: in `modelModify` push a pose, `poseStack.mulPose(new Quaternionf().rotateTo(new Vector3f(0,0,-1), dir))` mapping -Z onto the crystal→target-center unit direction, render, then pop — placed after `super.modelModify` so the model's separate self-spin pose never skews the beam. Confidence: 0.8
- When asked to make an orbiting/attached entity always face the player while circling, and told how to do it ("通过getRenderNode去实现"), the facing logic belongs in the `getRenderNode(partialTick)` override itself — not in the orbit-node provider (`getOrbitNode`) and not in the renderer: the override recomputes the interpolated orbit position, derives yaw/pitch from the owner's bounding-box center minus that position (`atan2(-dir.x, dir.z)` / `asin(-dir.y)` in degrees), adds a time-based roll self-spin (`(tickCount + partialTick) * rate % 360`), and returns the full `PathNode(pos, yaw, pitch, roll)` — the renderer then just consumes the node pose (via `super.modelModify`). Confidence: 0.6
- Toolchain fact for this repo: the Gradle 8.8 build cannot (re)run its build script under Java 25 (Groovy crashes with "Unsupported class file major version 69" the moment the configuration cache misses), so compile verification must set `JAVA_HOME` to the user's JDK 21 (`C:\Users\l1518\.jdks\jbr-21.0.11`) — the same JDK the IDE/Minecraft dev client run under; other JBRs on the machine (IDEA's jbr-25, Oracle javapath) break the build. Confidence: 0.9
- The user commonly keeps the Minecraft dev client running in the IDE while requesting code changes; that process locks `build/moddev/artifacts/neoforge-21.1.238.jar`, so while it runs, compile verification must not rewrite that artifact — use `gradlew compileJava --offline -x processResources -x createMinecraftArtifacts` (the artifact task re-runs on any JDK switch and then fails on the file lock). Confidence: 0.8
- When the user pastes another mod's code as a rendering reference (e.g., Create's BasinRenderer for a cauldron/crucible), only its positional/layout concept is to be borrowed — items piled/scattered over the fluid surface, fluid level rising with fill — and re-implemented with this project's own available APIs; the referenced mod's API is explicitly NOT usable ("这是其他模组Api不能通用，只参考渲染位置和堆积模式"). Confidence: 0.6
- For block-entity-rendered fluid inside a container, the user wants only the TOP surface drawn (no side walls, "流体只需要渲染顶面"); the surface quad must wind counter-clockwise when viewed from above (geometric +y normal, e.g., order `(x0,z1)→(x1,z1)→(x1,z0)→(x0,z0)`), otherwise `RenderType.translucent()`'s backface culling hides it — symptom phrased as "渲染反了/被反向剔除了". Confidence: 0.6
- When a fluid container's own block model is translucent (texture with alpha / `"render_type": "translucent"` in the model json, e.g., a glass crucible), a semi-transparent fluid surface alone lets the player see straight through the vessel to the background — the fix mirrors vanilla's solid cauldron + translucent water: draw an opaque backing under the fluid (e.g., a `RenderType.solid()` bottom quad at the interior floor), and float piled items ABOVE the fluid level rather than sinking them below it. Confidence: 0.6
- User refactoring style: prefers anonymous inline subclasses for small storage/fluid handlers — a field like `new ItemStackHandler(18) { protected void onContentsChanged(int slot) {…} }` / `new FluidTank(2000) {…}` — over dedicated named inner handler classes; after the user performs such a rewrite (deleting the inner class), fix leftover compile errors by keeping the anonymous fields and re-adding the missing plain getters (`getItemHandler()`/`getFluidTank()` returning the anonymous field) and adjusting references, not by reverting to the deleted named class. Confidence: 0.5
- Summoned magical/minion entities whose models are imported get dedicated renderers drawing the model full-bright regardless of scene light (`RenderUtil.FULL_LIGHT`, white tint × alpha), with a time-based Y self-spin and a translate centering the model's geometric center on the node origin ("使用全亮度"). Confidence: 0.5
- Expects complete bilingual (en/zh) translation coverage in the central data-gen provider (`FargoSoulLangProvider`): when asked to "add all translation names for X" (here, every registered mob effect keyed `effect.fargo_soul.<name>` via `entry(key, en, zh)`), every registered entry must receive its name line — missing ones are treated as gaps to fill in one pass (new effects get their en name derived from the register key plus a zh name), not left without names. Confidence: 0.4
- In the mod's own data-gen lang provider, translations should key off references to the registered variables instead of hand-written string keys ("使用effect方法引用变量来添加翻译"): mob-effect name lines go through a typed helper `effect(FargoSoulMobEffectRegister.XXX, en, zh)` — which internally derives the lang key from the DeferredHolder's `getDescriptionId()` — never raw `entry("effect.fargo_soul.<name>", ...)` literals, so lang keys cannot drift out of sync with the actual registrations; the user enforces this by editing in the helper method themselves and directing all existing entries be converted to it in one pass. Confidence: 0.7
- When mob-effect HUD icons are missing, the user directs generating simple 16×16 pixel-art icons programmatically with a tool/script rather than leaving them without art or hand-authoring them ("使用工具生成生物效果像素贴图，16*16大小，放置在正确的资源包中即可生效"): PowerShell + System.Drawing per-pixel drawing into `assets/fargo_soul/textures/mob_effect/<registration-id>.png`, filenames matching the `FargoSoulMobEffectRegister` field names exactly (Minecraft auto-loads effect icons from that path by effect ID, so no extra wiring is needed), one representative themed glyph per effect. Confidence: 0.5
