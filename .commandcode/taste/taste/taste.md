# Taste
- Communicates in Chinese (Simplified) when giving task instructions; expects responses in the same language. Confidence: 0.7
- During refactoring, wants changes scoped narrowly to the requested class/component (e.g., "不需要处理这个类以外的错误") — unrelated compile errors from other incomplete parts of the refactor should be treated as out of scope, not fixed or chased down. Confidence: 0.9
- Looks to sibling/similar projects (e.g., the Summoner mod) as the authoritative pattern reference when implementing comparable features (e.g., item registration) and expects the code to follow that established pattern ("参考同类项目Summoner的注册模式"); will actively point out the reference project's location when the assistant cannot find it — including the in-house Lyra framework's own source repo (D:/IDEA/lyra) when the assistant is about to decompile its jar instead of reading the real source. Confidence: 0.9
- Prefers using the shared Lyra library's helper builders (e.g., LyraItemRegisterBuilder) to register items and to drive generation of the related language-file/tooltip entries, instead of hand-written registration code plus separate manual lang-provider code. Confidence: 0.7
- Within Lyra's registration builder, prefers its dedicated typed methods over composing raw lang keys by hand — e.g., use `itemLanguageTooltip(index, en, zh)` for tooltip lines so the dependency library auto-generates the `item.<modid>.<path>.tooltip.<index>` key, rather than writing the key string manually. Confidence: 0.8
- When a dependency library natively auto-handles a runtime feature (e.g., Lyra's TooltipHandler automatically appends every `item.<mod>.<path>.tooltip.<N>` key, numbered contiguously from 1, to an item's tooltip), prefers consolidating all per-item text into that library mechanism — including merging previously shift-gated `attribute.N` skill text into the continuous `.tooltip.N` sequence — instead of keeping the consuming mod's parallel hand-rolled display logic that would duplicate output. Confidence: 0.6
- When organizing large registrations of hierarchical items, wants the register file itself laid out in ordered, named comment-banner segments mirroring the mod's item hierarchy (each force-level's sub-stones grouped as one segment, all force-level stones as one segment, and the Terra Soul as the final soul segment) rather than a flat listing. Confidence: 0.6
- Before undertaking large-scale migrations/refactors with high rework cost, prefers the assistant to surface architectural forks and ask clarifying questions with concrete options (e.g., how to handle missing types, key-naming conflicts, where segmentation should live) rather than guessing. Confidence: 0.8
- For large content migrations (e.g., moving hundreds of lang entries from a provider into a registration builder), insists the moved content be verbatim — preserve existing keys and exact en/zh text, do not fill in gaps, rename keys, or "improve" text while relocating. Confidence: 0.8
- When a requested rename would cascade across asset files (textures), recipes, and hardcoded code references (e.g., renaming every registration id to match English names with `_enchantment`/`_power`/`_soul` suffixes), prefers to keep internal registration identifiers stable and limit the change to the user-visible display-name/translation layer instead ("不改注册名，只改翻译名称"). Confidence: 0.7
- Wants English display names to be systematically consistent with the code's class/registration-name root plus a type suffix (BlazeSoul → "Blaze Enchantment", DeathPower → "Death Power", TerraSoul → "Terra Soul") rather than lore-based Terraria names ("Solar Enchantment", "Power of Death"); treats the Chinese display layer as an independent one that stays untouched when only English names are realigned. Confidence: 0.6
- Organizes creative-mode tabs via the sibling project's tag-driven Lyra Section mechanism (a `Section` record with order/banner/AnimBanner/tag, registered through `CreativeTabDispatcher.register(tab, Section...)`) instead of manually populating `displayItems`; each section collects its items purely from an item tag, so every item that must stay visible in the tab (including non-accessory materials/blocks) gets a section tag and non-article items get their own "basic" section (order 0). Confidence: 0.8
- Items that equip through Curios need the Curios slot item tag (`.itemTag(curios:curio)`) added in the Lyra builder chain on top of implementing the curio interface, because Curios slot fitting is tag-driven; follows the sibling project's accessory builder ordering — `.itemTag(section)` placed near the language lines and `.itemTag(Curio)` just before `.build()`. Confidence: 0.7
- For soul/accessory item tooltips, wants shift-gated display: with Shift NOT held show only a hint line ("按住 [Shift] 可查看概要", reusing the existing `key.shift.tooltip.1/2` keys with a gray literal "Shift" between them); full details only appear while Shift is held. Confidence: 0.8
- Shift-detail layout for hierarchical soul items: the first line enumerates ALL direct child soul items by their translated names in a "获得xx魔石、xx魔石……的效果" style (direct children from `getSoulItemList()`, not flattened descendants), then a blank separator line, then the item's own skill/stat lines; the own lines must follow the Lyra tooltip format — contiguous `item.<ns>.<path>.tooltip.N` keys read from 1 until the sequence breaks, rendered DARK_GRAY. Confidence: 0.7
- Implements tooltip display by putting the logic in the item class itself as a `getTooltip(TooltipFlag)` method on SoulItem and routing Lyra's tooltip through it via a client Mixin that intercepts the Lyra `TooltipHandler` output for these items — rather than a NeoForge `ItemTooltipEvent` listener or logic scattered in a client event class; display lives in the item, per-line text data stays in Lyra-generated `.tooltip.N` lang keys. Confidence: 0.7
- Treats the per-item language/tooltip text recorded in the register file (e.g., FargoSoulItemRegister) as the authoritative behavior specification for that item — "根据FargoSoulItemRegister中的语言文本完成代码" means implementing exactly what the (zh) tooltip lines describe (values, thresholds, cooldowns, passives), even when the old pre-refactor implementation of the same item did something different; outdated code is not a reference, the lang text is. Confidence: 0.7
- When completing a class in the post-refactor architecture, implement it by mirroring the already-migrated sibling classes (the sub-soul pattern: inner `SoulInfo` state class registered in `SoulInfoRegister`, tick-driven accumulation with `SoulItemData.isEquipped` removal, NBT serialize/deserialize, ability hooks as `SoulItem` overrides such as attack/hurt/curioTick) rather than the pre-refactor architecture's leftover patterns. Confidence: 0.7
- Names the per-soul inner state classes uniformly `Info` with no soul-name prefix (e.g., `BlazeSoul.Info`, `CosmicPower.Info`, never `BlazeSoulInfo`/`CosmicPowerInfo`), including the generic references in `SoulInfoRegister`; enforces this across the whole soul family when touching it. Confidence: 0.9
- Treats the inner `Info` (SoulInfo) classes as pure data stores: their `tick` should only check removal conditions (e.g., item no longer equipped → `setRemove(true)`) to prevent memory leaks, and must not carry gameplay/business logic ("info的功能主要是辅助数据存储，tick检查条件remove防止内存泄漏，不应该放置过多逻辑"). Confidence: 0.9
- Draws a semantic distinction for the removal condition in `Info.tick`: cooldown-type Infos (e.g., StardustSoul/VortexSoul — a skill-triggered cooldown timer) must NOT be removed on unequip; their tick is only the natural countdown (`--cooldown <= 0 → setRemove(true)`), so the cooldown persists on the entity through unequip/equip and cannot be refreshed by unequipping and re-equipping ("冷却类使用不应该在卸下后移除"). Only persistent/resource-type Infos (energy states like CosmicPower/BlazeSoul/NebulaSoul) bind removal to `SoulItemData.isEquipped`. Confidence: 0.8
- Wants gameplay/business logic relocated from the Info classes into the owning `SoulItem`'s overrides — per-tick behavior (energy accumulation/drain, cooldown countdown, healing) lives in `SoulItem.tick(LivingEntity)`/`attack`/`hurt`, not in `Info.tick`; a structural refactor must preserve the original behavior exactly (only move code, not change values/mechanics). Confidence: 0.7
- Damage-modifier semantics (current final state, supersedes the two earlier conventions): the user explicitly rejected distinguishing "original vs latest" damage bases as too troublesome ("区分原始和最新还是太麻烦了"). `SoulItem.attack/hurt` hooks now carry only the running chained value (`damage`) — the `originalDamage` parameter was removed from the base class and all cosmic-power overrides. Every percentage effect is multiplicative on the current value and worded accordingly: boosts are "提升至 X%" (`damage *= X/100`, e.g. Stardust frozen "造成的伤害提升至200%" = `damage *= 2`; CosmicPower "最高提升至180%" = `damage *= 1 + 0.8*ratio`); damage reduction stays "减少 X%" (`damage *= 1 - X%`); only fixed-value additions (flat amounts like Nebula's "附加你4%最大生命值") remain additive because they are not percentage boosts. Tooltip wording is updated in lockstep with code semantics. Confidence: 0.9
- The damage-base convention flip-flopped across sessions (original-based additive → latest-value "提升至 X%" → original-based → back to latest-value "提升至 X%"); treat the latest-value-multiplicative system as settled and do not re-introduce an `originalDamage` parameter or an original-vs-latest distinction without an explicit new instruction. Confidence: 0.8
- Prefers simple, uniform mechanic designs over precise-but-complex ones: chose one uniform multiplicative base over a semantically exact dual-base system because maintaining two semantics was too troublesome. Favor the least-complex consistent rule when proposing damage-modifier/mechanic designs. Confidence: 0.7
- Implements special effect entities (non-rendered, summoned-by-item effects like the Vortex) as Lyra "virtual entities" (the project's self-developed attachment-entity system, no renderer needed) rather than vanilla/NeoForge rendered entities; points to an existing in-project sibling class (Meteor) and the Summoner sibling project as the authoritative reference for how such entities are structured. Confidence: 0.8
- Treats entity-type registration and render registration as a required binding pair for every virtual entity class in common/entity (SummonerAttachmentEntityRegister + SummonerAttachmentEntityRenderRegister); pure logic/auxiliary entities (e.g., Sprint, used only for sprint-assist collision detection) are explicitly bound with a null renderer ("不进行渲染") rather than being skipped in the render register or given a visual renderer. Confidence: 0.7
- Wants particle effects driven through the Lyra particle API — `ParticleHelper` controlling `GenericParticleBuilder` — rather than vanilla particle spawning. Confidence: 0.6
- Data-ownership rule for the SoulInfo/attachment sync layer, stated as a two-sided split: pure-client data is client-local only and must survive server sync/deserialization (server data must never overwrite or clear it) and must be excluded from server-side NBT saves and sync packets; ALL non-pure-client data, by contrast, is fully cleared and rebuilt from the server snapshot so server-removed entries cannot linger on the client — the client's resulting state is "client-owned data preserved + server snapshot replaces everything else" ("纯客户端数据由客户端自己控制，其他数据完全清理后由服务端数据覆写"). Confidence: 0.8
- The ownership marker is a type-level property, not per-instance state: explicitly directed to move the pure-client flag off the instance method (`SoulInfo.isClientSide()` deleted) onto the registered type itself — a `clientSide` boolean component of the `SoulInfoType` record set at registration (Sprint/Fly registered as `clientSide=true`). Tick dispatch and (de)serialization/sync filtering all read the flag from the entry's type key, since data ownership belongs to the data kind, not to individual instances. Confidence: 0.9
- The server-side tick handler that mutates the SoulInfoData attachment is the point where removals must be pushed: after the `removeIf` sweep of expired entries (each entry `tick()`ed, removed when `isRemove()`, with `onRemove()` called before removal), the handler ends with `living.syncData(SOUL_INFO_DATA)` — explicit sync at the mutation site rather than relying on login/initial attachment sync, since mid-session server removals otherwise never reach the client and linger there. Confidence: 0.8
- The tick handler must run on BOTH logical sides, each side ticking only the data kind it owns — server ticks only `clientSide=false` entries, client ticks only `clientSide=true` entries (`soulInfo.getType().clientSide() != clientSide → skip`), with `syncData` fired only from the server side; the two sides manage disjoint data and never tick each other's types. Confidence: 0.9
- Communicates the expected implementation by pasting a concrete, runnable code snippet as the whole message (no prose); the pasted code is the spec to adopt and build on as-is (here: `EntityTickEvent.Post` guarded server-side via `instanceof LivingEntity` + `!level().isClientSide()`, `removeIf` over the attachment's `data().values()`) rather than something to redesign or replace. Confidence: 0.6
- For a deserialize path shared between client sync and server NBT load, the "clear non-client-owned, keep client-owned, rebuild from incoming data" rule is safe on both sides precisely because the server never holds client-only types in the first place — filtering by the type flag in `deserializeNBT` yields identical, correct semantics when reading either a sync packet (client) or a save file (server). Confidence: 0.6
- The behavior-preservation rule applies generally to cleanup rewrites of leftover old-architecture code inside new-architecture classes, not just to Info/game-logic relocation: rewriting SoulRarity's tick-driven gradient coloring for special rarities (-1 Expert / -2 Master) must keep the observable final effect identical ("在不改变最终效果的情况下进行重写"), while the user explicitly invites finding a more concise formulation in the process ("看看有没有更简洁的写法"). Confidence: 0.7
- For derived per-tick values that vary by a category key (e.g., rarity gradient colors for -1/-2), prefers the computed results to be stored in a map keyed by that category ("改为写入map") rather than held per-instance or recomputed through imperative branches in tick. Confidence: 0.5
- Keeps display-only animation strictly separate from persisted data-component state: purely visual dynamic effects (e.g., animated gradient colors for sentinel rarity values -1/-2) must never mutate the stored component — the record's canonical accessor (`color()`) continues to return the true persisted value (the Codec/StreamCodec serialize through it, so overriding it to return an animated color would corrupt saves/sync), while display reads go through a separate getter (`getColor()`) that resolves the client-side cached display color; the client-only tick updates only that cache, never the component ("我改为了getColor防止组件真实数据改变，此颜色只作为展示效果，不改变真实数值"). Confidence: 0.9
- Naming style for static constants: prefers PascalCase (like `Purple`, `Expert`, `Master`) over ALL_CAPS / UPPER_SNAKE_CASE (e.g., GRAY, CODEC, STREAM_CODEC) — explicitly instructed to rename SoulRarity's constants to "像Purple这样的格式，不要全大写", including naming bare sentinel literals (-1/-2) as named constants in the same style. Confidence: 0.7
- Tooltip lang-key index sign convention in this mod's soul items: positive indices (1, 2, 3…) are the Shift-held mechanic/description lines (the `getTooltip` reads them ascending from 1 when Shift is down), while negative indices (-1, -2, -3…) are the easter-egg/atmosphere quote lines shown when Shift is NOT held, read descending from -1 (`for (int i = -1; I18n.exists(key + i); i--)`). When the user says "逻辑端已经完成" and pastes that getTooltip, the pasted logic is the authoritative spec and register/lang data must be renumbered to match it — easter-egg lines go to -1, -2, … (multi-line eggs keep their reading order as -1 then -2, matching the descending loop), never left on positive indices. Confidence: 0.7
- For per-player damage-source identity of a soul item, wants the UUID to be a deterministic derivation from BOTH the soul item and the player rather than a bare player UUID ("以SoulItem和玩家uuid构建一个稳定的uuid") — same player + same soul item must always yield the same stable UUID (unchanged across reconnects/re-entry), while different items or different players yield distinct values so each soul's damage can be tracked independently (e.g., per-UUID i-frames). Confidence: 0.6
- For effect-entity visuals when the mod lacks a dedicated texture/model asset, prefers to skip sprite rendering entirely ("暂时不渲染贴图") and convey the effect with light/particles only — e.g., the shadow orb renders through a light-only renderer (`simpleRenderer(null, 0, 12)` → only `DynamicLightDispatcher.addLightSources(pos, 12)`) rather than reusing an unrelated existing texture (like the Meteor glow sprite tinted purple) as a placeholder; dedicated visual assets can be added later. Confidence: 0.5
- Code style: avoid extracting single-use constants for gameplay values/durations when not necessary — inline literals directly ("非必要不写常量（如持续时间、数值等直接内联字面量）"), instead of hoisting e.g. `PREEMPTIVE_DURATION = 20` / `ARMOR_BREAK_DURATION = 200` style named constants for one-off usages. Confidence: 0.7
- Comments should stay minimal: no long javadoc blocks on classes/methods ("类/方法上的大段 javadoc 不需要"); at most keep a single short explanatory comment line before registration entries ("注册项前可保留一行简短说明"), e.g., a one-line zh note describing the effect above its effect/item registration. Confidence: 0.8
- When the user personally rewrites an implementation and tells the assistant to learn their style ("我修改了代码，你需要学习我的写作风格"), the hand-edited code becomes the authoritative spec: do not revert or "fix" the mechanics back to the previous implementation; instead confirm any ambiguous semantics (e.g., changed damage multiplier, moved trigger point) via targeted questions and align stale comments, register values, and tooltip text to the user's version. Confidence: 0.7
- Feature ownership across soul items: when a soul's effect only conditions on a status that a different soul applies (e.g., DarkArtistSoul benefits against shadow-flame targets while applying shadow flame is GloomySoul's role), implement the soul as purely conditional — do not make it self-apply the prerequisite effect just to be self-sufficient, even if that leaves the condition dormant until the supplying soul is finished. Confidence: 0.6
- Code style: generally avoids early `return` exits in favor of nested `if` structures ("一般不使用提前 return 提前退出，而使用 if 嵌套结构") — e.g., instead of a top-of-method `if (!condition) return;`, wrap the logic in `if (condition) { ... }`. Confidence: 0.8
- Implements continuous damage-over-time / health-drain status effects via the lyra HealthRegen attribute with a negative curve modifier (`amplifier -> -(amplifier+1)*系数`) rather than manually calling `hurt` in `applyEffectTick` gated by `shouldApplyEffectTickThisTick`. Confidence: 0.8
- For latency-sensitive ability state that must apply on the next action without waiting for a packet round-trip (e.g., "press penetrate key → the next sprint becomes phase-through"), has BOTH the client and the server hook of that action write the same self-decrementing marker — the client writes first ("client 提前添加，防止同步不及时") so the effect is locally active before the server's copy exists, and the server writes when the packet arrives. Confidence: 0.7
- For such dual-side ability states, prefers ONE server-authoritative (`clientSide=false`) SoulInfo that syncs to the client over maintaining two parallel client/server info types: the client may locally write pending/timer fields in advance (in `sprintClient`) and the server's later `sprintServer` write + syncData overwrite reconciles them; the small race window this implies is accepted by design. Confidence: 0.6
- Implements block-phase / pass-through abilities as empty collision boxes: inject into `BlockBehaviour.BlockStateBase#getCollisionShape` via a matched pair of mixins — `ClientBlockStateBaseMixin` (matches LocalPlayer) and `ServerBlockStateBaseMixin` (matches ServerPlayer) — and return `Shapes.empty()` when the player's active-state info (queried via SoulInfoData) says the effect is on, so both logical sides agree on wall-ignoring. Confidence: 0.7
- For revive-on-death mechanics hooked into a cancellable death event, first verifies the death was NOT already canceled by another effect (`canceled == false`) before consuming the revival resource and restoring health; only after that check passes does the hook itself cancel the death (return false) — don't burn the revive on an already-prevented death. Confidence: 0.6
- Issues extremely terse, single-phrase rework directives for a soul item (e.g., "NinjaSoul重做。") with no spec details whatsoever; expects the assistant to autonomously derive the complete rework target from authoritative in-repo sources — the item's (zh) tooltip/lang text in the register file, which can describe an entirely different mechanic than the current code — and then implement, register the state type, and compile-verify end-to-end without asking clarifying questions for such a scoped item rework. Confidence: 0.5
